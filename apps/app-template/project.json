{
  "name": "app-template",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/app-template",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/app-template",
        "outputPath": "dist/apps/app-template"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/app-template"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "app-template:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "app-template:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "app-template:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "app-template:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/app-template"],
      "options": {
        "jestConfig": "apps/app-template/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/app-template/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
